// Rendering
export function setupMovesTable() { /* Render and bind click handlers */ }
export function updateMoveTable(filteredMoves = []) { /* Update and filter moves dynamically */ }

// Interaction Handling
export function handleMoveSelection(moveIndex, mode) { /* Trigger playback with selected mode */ }
